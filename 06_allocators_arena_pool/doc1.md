

## **6.1 引言：内存管理的理论框架**

现代 C++ 程序在运行时大量依赖动态内存分配（`new`/`delete` 或 `malloc`/`free`）来管理对象生命周期与数据存储。标准的默认分配器通常是用系统 `malloc/free` 实现（如 glibc 的 ptmalloc、macOS 的 libmalloc），它们通用但在**高频分配（small allocations）、多线程环境下效率低下且碎片化严重**。因此，通过自定义分配器可以获得显著性能提升（如 2–20×）——特别是在游戏引擎、数据库、网络服务器等高分配场景。具体技术涉及堆管理、碎片分析、线程安全优化与策略设计（池化、伙伴系统、线程本地分配等）。

> **参考文章**（分配器总体概览与比较）：libmalloc、jemalloc、tcmalloc 与 mimalloc 的对比介绍（免费开源说明）([DEV Community][1])

---

## **6.2 默认分配器的理论缺陷**

### **6.2.1 默认 new/delete 与 malloc/free 的实现关系**

* `operator new` 通常内部调用 `malloc`，然后调用构造函数；`operator delete` 调用析构后执行 `free`。
* 默认 allocator 不提供针对特定大小/生命周期的优化，往往设计为“通用适用”而非性能最高。

### **6.2.2 多线程争用与全局锁**

### **问题**

默认实现通常在内部使用**全局锁或共享锁**来保护堆元数据结构，以保证多线程安全，但导致多线程下**锁竞争严重**、延迟高。

### **碎片化问题**

分配器需要管理不同大小的分配请求，可能导致：

* 外部碎片（free block 无法组合成大块）
* 内部碎片（分配给 caller 的块比实际请求大）

内存碎片率高可超过 30%，影响缓存局部性与可用内存效率。通用 allocator（如 glibc ptmalloc）往往采用混合策略（best-fit + free lists），但仍无法为所有场景优化。

> **说明**：allocator 内部实现策略直接影响碎片化（best-fit/worst-fit 等算法）([CS341][2])

---

## **6.3 自定义分配器接口理论**

### **6.3.1 C++ 标准 Allocator**

C++ 标准库定义了 **Allocator 概念**，以模板方式抽象内存分配与对象构造：**allocate / deallocate** 接口。

* `std::allocator<T>` 是默认 allocator。
* 你可以定义自己的 allocator 类型，并在容器中传入它以替换默认行为。
* 标准层面，allocator 负责 raw memory 分配，而对象构造/析构仍由容器/调用者完成。

C++ 标准最初目标是让 allocator 抽象底层内存模型，但后来因性能 & 可实现性原因，设计限制了某些抽象能力。

> **参考**：Wikipedia “Allocator (C++)” 介绍 allocator 目的与使用情形([维基百科][3])

### **6.3.2 C++17 PMR（Polymorphic Memory Resource）**

* 提供 `std::pmr::memory_resource` 抽象与 `polymorphic_allocator`。
* 允许**运行时替换**分配策略，而不影响模板参数体系。
* 结合策略模式 + 零开销抽象，可根据不同分配模式（池、arena、栈式等）切换资源。

### **学习视频**

🎥 **C++ Weekly，PMR 系列讲解**：

* *Ep. 248 — Understand C++17 PMR and allocator* ([YouTube][4])
* *C++Now 2018: C++17 Polymorphic Memory Resources* ([YouTube][5])
* C++ PMR 系列播放列表（多部）([YouTube][6])

> 这些视频解释了 PMR 如何提供策略化内存资源与 allocator 之间的关系，是理解自定义分配的接口基础。

---

## **6.4 池化分配器（Pool Allocator）理论**

### **6.4.1 模型与基本思想**

**池化分配器**预分配一大块内存，并划分成**固定大小块（fixed-size blocks）**用于分配固定对象（例如粒子对象等）。常见策略：

* Free list（空闲链表）管理可用块
* Bitmap（位图）用于快速定位空闲块
* O(1) 分配/释放，不涉及锁/全局竞态

这是非常经典的内存池方法，适用于对象大小固定、生命周期相似的场景。

> **Wikipedia：Memory Pool 定义与固定大小块说明**([维基百科][7])

### **6.4.2 优点**

* 速度快（O(1) 分配）
* 几乎无碎片（固定大小）
* 可避免频繁调用系统 malloc/free

### **6.4.3 实现示例与文章资料**

**中等难度教程**：内存池的思想与常见实现（包括 FreeList、链表等），对初学者非常有用。([Medium][8])

---

## **6.5 伙伴系统（Buddy System）理论**

### **6.5.1 设计原理**

伙伴系统将堆分成 2^k 大小的块：

```
Sizes: 2^0, 2^1, 2^2, ..., 2^k
```

分配时按 2^k 向上取整；释放时可与相邻“伙伴”合并成更大块，从而减少碎片。

优点：

* 容易合并
* 较优碎片控制
* 多线程友好

### **6.5.2 理论分析**

在大规模堆中，伙伴系统减少了碎片合并的复杂性（近似二叉树分布），且可以通过 bitmaps/位图更快地管理分配状态。它是 jemalloc 等实际 allocator 的基础组件之一。

> jemalloc 等高级 allocator 部分采用伙伴系统来分配较大块内存；这是它们减少碎片的重要因素。([DEV Community][1])

---

## **6.6 线程本地分配（TLA）理论**

### **6.6.1 谷歌 TCMalloc 设计分析**

tcmalloc（Thread-Caching Malloc，从 Google Performance Tools 演化而来）通过设计**线程本地缓存**减少跨线程锁竞争：

* 每个线程有自己的小对象缓存
* 当线程独占请求时无需锁竞争
* 中央堆（Central Heap）只在本地缓存空时访问

这样在多线程下，分配速度可提高数倍（5–10×），避免全局锁瓶颈。

**层次模型：**

1. **ThreadCache（线程缓存）**：本地快速路径
2. **Transfer Cache**：跨线程中转缓存
3. **Global Page Heap**：与操作系统交互获取大页

实现参考架构可见 Medium 组装 allocator 例子（该文章展示了 tcmalloc 灵感的三层架构）。([Medium][9])

---

## **6.7 SOTA 分配器对比理论**

| 分配器          | 关键机制                            | 优点          | 弱点      |
| ------------ | ------------------------------- | ----------- | ------- |
| **jemalloc** | Size classes + TLA + 自动碎片回收     | 线程友好、碎片低    | 稍重/复杂   |
| **tcmalloc** | 线程缓存 + 中央堆                      | 多线程速度快      | 可能碎片更高  |
| **mimalloc** | Sharded free lists + page reset | 低延迟、优秀缓存局部性 | 新、优化空间大 |

### **实践案例**

* 有实际 benchmark 指出 Redis 使用 jemalloc 后，内存分配性能提升显著（例如速度提升 ~3×）。这是实际系统采用 SOTA allocator 的典型例子。

### **各实现开源链接**

* **jemalloc**：FreeBSD 发起，更广泛用于高性能服务器。
* **tcmalloc**：Google 开源工具集的一部分。
* **mimalloc**：微软研究院出品，专注性能与低延迟。

> mimalloc 是 Microsoft 研究推出的通用 allocator，支持 free list sharding 等现代技术实现。([维基百科][10])

---

## **6.8 自定义实现理论挑战**

设计自己的 allocator 并不简单，需要考虑：

### **对齐需求**

现代 SIMD 等架构要求 64B/128B 对齐，否则性能损失甚至崩溃。

### **安全性**

安全 allocator 可能要求内存**清零**、防止数据泄露等机制，在密码学/安全系统中尤为重要。

### **监控与统计**

需要钩子（hooks）来跟踪分配行为、内存使用统计、泄漏检测。

### **复杂性与维护成本**

自己写 allocator 除了分配逻辑，还要处理各种 corner cases，实际非常困难。业内有论文说明自定义 allocator 不简单，可能需要大量工程投入。([arXiv][11])

---



## **6.9 小结**

动态内存分配是系统性能的隐性瓶颈。默认 allocator 灵活但效率与碎片控制较弱。通过理解分配理论（池化、伙伴、TLA、策略抽象等），可以：

* 针对应用场景设计或选择合适 allocator
* 显著降低碎片与锁竞争
* 提升系统整体性能

这种理论知识对性能工程师、堆管理实现者至关重要。

---

## **推荐学习资源（视频 + 文档）**

### 📺 视频（概念 + 实例）

📌 **C++ Weekly Ep.248/250 — PMR & 自定义分配器**（YouTube）([YouTube][4])
📌 **C++Now 2018 — PMR 深度讲解**（YouTube）([YouTube][5])
📌 **Basic allocator-aware types in C++**（YouTube）([YouTube][12])
📌 **Video 讲解 std::pmr::polymorphic_allocator**（YouTube）([YouTube][13])

### 📄 文章与博客

🔗 **libmalloc, jemalloc, tcmalloc, mimalloc 比较**（Dev.to）([DEV Community][1])
🔗 **Allocator (C++) 提供标准定义与用途**（Wikipedia）([维基百科][3])
🔗 **Memory Pool 理论与用途**（Wikipedia）([维基百科][7])
🔗 **构建内存分配器的深入文章（Medium）**([Medium][9])

---


[1]: https://dev.to/frosnerd/libmalloc-jemalloc-tcmalloc-mimalloc-exploring-different-memory-allocators-4lp3?utm_source=chatgpt.com "libmalloc, jemalloc, tcmalloc, mimalloc - Exploring Different Memory ..."
[2]: https://cs341.cs.illinois.edu/coursebook/Malloc?utm_source=chatgpt.com "Malloc - CS 341"
[3]: https://en.wikipedia.org/wiki/Allocator_%28C%2B%2B%29?utm_source=chatgpt.com "Allocator (C++)"
[4]: https://www.youtube.com/watch?v=Zt0q3OEeuB0&utm_source=chatgpt.com "C++ Weekly - Ep 248 - Understand the C++17 PMR ... - YouTube"
[5]: https://www.youtube.com/watch?v=FLbXjNrAjbc&utm_source=chatgpt.com "C++Now 2018: David Sankel “C++17's std::pmr Comes With a Cost”"
[6]: https://www.youtube.com/playlist?list=PLs3KjaCtOwSYX3X0L36NgwK0pxZZavDSF&utm_source=chatgpt.com "PMR And Allocators - YouTube"
[7]: https://en.wikipedia.org/wiki/Memory_pool?utm_source=chatgpt.com "Memory pool"
[8]: https://medium.com/%40threehappyer/memory-pool-techniques-in-c-79e01f6d2b19?utm_source=chatgpt.com "Memory Pool Techniques in C++. 1. Preface Memory management is a… | by happyer | Medium"
[9]: https://medium.com/%40kondam.reddy/a-deep-dive-into-building-a-memory-allocator-dd5333a98195?utm_source=chatgpt.com "A Deep Dive into Building a Memory Allocator | by Sriman | Nov, 2025 | Medium"
[10]: https://en.wikipedia.org/wiki/Mimalloc?utm_source=chatgpt.com "Mimalloc"
[11]: https://arxiv.org/abs/2206.11728?utm_source=chatgpt.com "There Ain't No Such Thing as a Free Custom Memory Allocator"
[12]: https://www.youtube.com/watch?v=2LAsqp7UrNs&utm_source=chatgpt.com "C++ Weekly - Ep 236 - Creating Allocator-Aware Types - YouTube"
[13]: https://www.youtube.com/watch?v=SD9TcKPyfvc&utm_source=chatgpt.com "Understanding the Purpose of std::pmr::polymorphic_allocator"
